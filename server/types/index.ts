export enum RegisterType {
  EMAIL = 1,
  NAVER = 2,
}

export enum PostStatus {
  IN_PROGRESS = 1,
  SUCCESS = 2,
  FAIL = 3,
}

export enum TransactionJobStatus {
  PENDING = 1,
  SUCCESS = 2,
  FAIL = 3,
}
